<p>update discount</p>

<%= form_with(model: @discount, local: true) do |form| %>
  <%= render 'error' %>

  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <p>
        <%= form.label :discount %><br>
        <%= form.text_field :discount %>
      </p>

      <p>
        <%= form.submit "update discount", class: "btn btn-primary" %>
      </p>
    <% end %>
  </div>
  </div>
  <%= link_to 'Back', discounts_path %>
